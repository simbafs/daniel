alert('hello');

console.log('submit.js loaded');
$('textarea').eq(1).autoResize();
let lastRemove = [];
let last = $('td').eq(-2).attr('id');
let remove = [];
/*
$('.remove').click(function(){
        let $target = $(this).parent().parent();
        console.log($target);
        if($target.hasClass('addition')){
                $target.children().eq(0).children()a.val('');
        }else if($.cookie("admin")){
                remove.push($target.attr(''));
                lastRemove.push($target.remove());
        }else{
                alert('You don\'t have the promission');
        }
});

$('form').submit((e) => {
        e.preventDefault();
});
*/
